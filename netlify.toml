[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# SPA redirect — all routes serve index.html so React Router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
